[user]
	email = tomsik68@gmail.com
	name = Tomas Jasek
[core]
	fileMode = false
	autocrlf = false
[alias]
    ds = diff --staged
    d = diff
    a = add
    au = add -u
    aui = add -u -i
    ai = add -i
    co = checkout
    cob = checkout -b
    cop = checkout -p
    cl = clone
    st = status
    br = branch
    c = commit -s
    cs = commit -s
    gud = push
    ll = pull
    l  = log --oneline
    ld = log -c --oneline --notes
    rb = rebase
    rs = reset
    sh = stash
    shl = stash list
    shp = stash -p
    shs = stash show -p
    shki = stash save --keep-index
    tr = log --oneline --decorate --all --graph
[url "https://aur.archlinux.org/"]
    insteadOf = "aur:"
[url "ssh+git://aur4.archlinux.org/"]
    pushInsteadOf = "aur:"
[url "https://github.com/"]
    insteadOf = "gh:"
[url "git@github.com:"]
    pushInsteadOf = "gh:"
[url "git@gitlab_fi:"]
    insteadOf = "fi:"
    pushInsteadOf = "fi:"
